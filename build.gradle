group 'io.lance.vert.x'
version '0.0.1'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

ext {
    testngVersion = '6.13.1'
    vertxVersion = '3.5.0'
}

dependencies {
    /*compile("io.vertx:vertx-core:${vertxVersion}")*/
    compile("io.vertx:vertx-core:${vertxVersion}")
    compile("io.vertx:vertx-web:${vertxVersion}")

    compile group: 'io.vertx', name: 'vertx-dependencies', version: '3.5.0', ext: 'pom'


    testCompile("org.testng:testng:${testngVersion}")
}
